function multiplicacao(multiplicador1: number, multiplicador2: number): number {
    return multiplicador1 * multiplicador2;
};

function saudacao(nome: string): string {
    return "Olá " + nome;
};

console.log(saudacao('Mundo'));